0. Besides 4×4(which are The Game of Fifteen’s dimensions), what other dimensions does
the framework allow?

The dimensions are defined by the constants DIM_MIN and DIM_MAX, 3 and 9 respectively. The side of the board can be
3 to 9 units.

1. With what sort of data structure is the game’s board represented?
The board is represented by a 2 dimensional array.

2. What function is called to greet the player at game’s start?
The greet function is called.

3. What functions do you apparently need to implement?
init(), draw(), move(), won()